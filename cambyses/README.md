# Cambyses — Entity Component System

**Status**: Placeholder — Implementation begins in Stage 3.

## Purpose

`cambyses` implements the Entity Component System (ECS) — the architectural backbone for game object management, component storage, and system execution.

## Planned Contents

- **core/** — ECS core types and configuration
- **components/** — Built-in components (transform, tag, etc.)
- **systems/** — System execution framework
- **queries/** — Component queries and filtering
- **archetypes/** — Archetype-based component storage
- **events/** — Event system integration
- **world/** — World management and lifecycle

## Dependencies

- `foundation` — Core utilities, memory, containers
